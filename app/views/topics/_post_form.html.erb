<%= form_for(@post, controller: 'posts', action: 'create') do |post| %>
  <%= post.hidden_field :topic_id, value: @topic.id %>
  <% if signed_in? %>
    <%= post.hidden_field :user_id, value: current_user.id %>
  <% end %>
  <%= post.text_area :content, value: "twoja odpowiedź" , rows: 4 %>
  <div class = "submit_button">
    <%= post.submit "odpowiedz" %>
  </div>


<% end %>
